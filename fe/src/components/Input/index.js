import React from 'react';

const Input = () => {
    return (
        <div>
            <input type="text" className="my-4" placeholder="Tìm kiếm trên TeZa" />
        </div>
    );
};

export default Input;
